name: Check CPP OpenCL Samples
run-name: ${{ github.actor }} is checking samples
on: [push]
jobs:
  Sanity-Check-CPP-OpenCL-Sample:
    runs-on: ubuntu-latest
    steps:
      - name: Build Info
        run: |
          echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
          echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
          echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v4
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - name: Check Cpp OpenCL Samples
        run: |
          sudo apt-get update
          sudo apt install libpocl2  intel-opencl-icd opencl-headers ocl-icd-opencl-dev clinfo
          ln -s $PWD TTL
          export TTL_INCLUDE_PATH=$PWD
          cd opencl/samples/cpp
          clinfo
          ./test_all_types.sh

      - run: echo "🍏 This job's status is ${{ job.status }}."
